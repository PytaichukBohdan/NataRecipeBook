[project]
name = "piapi-video-generator"
version = "1.0.0"
description = "Generate AI videos using PiAPI (Kling 2.5 Turbo) with start/end frame control"
requires-python = ">=3.10"
dependencies = [
    "requests>=2.28.0",
    "python-dotenv>=1.0.0",
]

[tool.uv]
dev-dependencies = []
