[project]
name = "nano-banana-pro"
version = "0.1.0"
description = "Image generation using Google Gemini API"
requires-python = ">=3.10"
dependencies = [
    "google-genai>=0.3.0",
    "python-dotenv>=1.0.0",
]

[tool.uv]
dev-dependencies = []
